{% if package.response.status == 200 and package.url.verb == 'display' %}
  {% for hero in cms.helper('graph').children(package.noun.dso.id,'hero') %}
    {% include 'digraph/hero.twig' with {
      hero: hero
    } %}
  {% endfor %}
{% endif %}
