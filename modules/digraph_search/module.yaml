helpers:
  initialized:
    search: true
  classes:
    search: ${module.namespace}\SearchHelper

search:
  cron:
    interval: 86400
    limit: 1
  highlight_length: 150

paths:
  searchindex: ${paths.storage}/searchindex

strings:
  urls:
    common:
      _search/display: Site search
